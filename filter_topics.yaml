# blacklist or whitelist mode for allowed topics.
# If mode is not specified, defaults to "allow_all".

filter_mode: "whitelist"

rules:
  agnocast_sample_interfaces/msg/DynamicSizeArray:
    bidirectional:
      - /my_topic
    # a2r:
    #   - /my_topic
    # r2a:
    #   - /my_topic